fastapi>=0.111.0
uvicorn[standard]>=0.29.0
sqlalchemy[asyncio]>=2.0.30
asyncpg>=0.29.0
alembic>=1.13.1
pydantic>=2.7.0
pydantic-settings>=2.3.0
apscheduler>=3.10.4
numpy>=1.26.4
python-dateutil>=2.9.0
httpx>=0.27.0
structlog>=24.2.0
psycopg2-binary>=2.9.9
yfinance>=0.2.38

# ---------------------------------------------------------------------------
# Optional dependencies (install separately as needed)
# ---------------------------------------------------------------------------
# IBKR delayed provider:
#   pip install ib_insync>=0.9.86
#   Requires TWS or IB Gateway running on the configured host:port.
#   See app/providers/ibkr_delayed.py for full setup instructions.
#
# Polygon provider:
#   No extra install — uses httpx (already in core deps above).
